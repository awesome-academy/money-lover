<div class="col-md-12">
  <div class="action mb-0">
    <%= link_to t("button.new"), new_admin_user_path, class: "btn btn-info" %>
  </div>
</div>
<div class="col-md-12">
  <div class="panel panel-primary mt-20">
    <div class="panel-heading">
      <%= t "table.title.users" %>
    </div>
    <div class="panel-body">
      <%= render "admin/shared/form_search", search: Settings.search1 %>
      <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover">
          <thead>
            <tr>
              <th>#</th>
              <th><%= t "table.head.name" %></th>
              <th><%= t "table.head.username" %></th>
              <th><%= t "table.head.email" %></th>
              <th><%= t "table.head.phone" %></th>
              <th><%= t "table.head.address" %></th>
              <th></th>
            </tr>
          </thead>
          <tbody id="table-user">
            <%= render "admin/users/user", users: @users %>
          </tbody>
        </table>
      </div>
      <div id="paginator">
        <%= paginate @users, theme: "twitter-bootstrap-3" %>
      </div>
    </div>
  </div>
</div>
